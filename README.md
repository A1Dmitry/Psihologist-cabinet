# Psihologist-cabinet

Портал кабинетов специалистов (SPA): каталог, запись, оплата/аванс, напоминания, перенос с согласием, шифрование PII.
В поисковых системах сайт позиционируется как **мультипрофильный портал для
специалистов (самозанятых, ИП) и клиентов/заказчиков** — психология,
программирование и другие виды деятельности самозанятых/ИП (ключевые слова,
мета-теги и JSON-LD — в `<head>` `index.html`, стратегия — раздел «SEO» ниже).

**Репозиторий (основной):** https://github.com/A1Dmitry/Psihologist-cabinet  
Перенос в `mikhailouskayanataliya-collab/Psihologist-cabinet` отложен: целевой
репозиторий недоступен (приглашение коллаборатору не принято/нет доступа).
Инструкции сохранены в `docs/archive/repo-transfer/` — статус: открытый вопрос
к владельцу продукта. До решения текущий репозиторий — единственный источник истины.

## Запуск

```bash
python3 devserver.py 8765        # SPA-fallback: deep-link /psy/{slug} → index.html + авто-нормализация в #/psy/{slug}
# или
chmod +x serve.sh && ./serve.sh  # простой статический сервер (только корень)
```

Открыть: http://127.0.0.1:8765/

Смоук маршрутов: `python3 verify_pages.py` (или `BASE_URL=https://… python3 verify_pages.py` для Pages),
роутер Hash History: `node test_routing.mjs`, воронка записи: `node tests/booking-wizard.mjs`.

## Структура

- `index.html` — UI
- `js/models` — Code First
- `js/core/dbContext.js` — хранилище
- `js/viewmodels` — MVVM
- `js/services` — auth, crypto, vault, payment, reminders, fraud, calendar (Google), seo
- `supabase/schema.sql` — DDL БД + RLS (аноним видит только публичное; клиенты — только владельцу; запись через RPC)
- `supabase/seed.sql` — референс-данные профиля с сайта nataliamikhailouskaya.by
- `docs/DATA-MODEL.md` — модель данных сайта, сверка и маппинг в БД
- `docs/ROADMAP-OKNA.md` — роадмап: сверка функций с okna.one и бэклог задач (T-01…T-25)
- `docs/ROADMAP-DATABASE-CONNECTION.md` — роадмап правильного подключения к базе данных (Supabase MCP, AI-клиенты, безопасность)
- `docs/INFRA.md` — продакшен-инфраструктура: runbook Supabase/Resend/Pages, статус, чек-лист
- `docs/SCHEMA-REQUESTS.md` — очередь заявок на изменение схемы (правила для агентов 2–5)
- `docs/RULES.md` — обязательные строгие правила работы с проектом (типология «туфты», запрет самосертификации, 12-пунктовый итоговый отчёт, §7 RRSI — контур самообучения исполнителей)
- `docs/RRSI-CACHE.md` — кэш правильных решений и невынужденных ошибок исполнителей (порядок применения — `RULES.md` §7)
- `AGENTS.md` — входная точка правил для ИИ-агентов (обязательное чтение до начала работы)
- `devserver.py` — локальный сервер со SPA-fallback (глубокие ссылки `/psy/{slug}`)
- `verify_pages.py` — смоук-проверка маршрутов (локально или GitHub Pages)

## Маршруты (индексируемые URL)

- `/` — каталог специалистов
- `/psy/{slug}` — страница специалиста: SEO-лендинг (профиль, услуги/цены, реквизиты, контакты; canonical, OG, JSON-LD schema.org)
- `/book/{slug}` — страница записи: услуга → время → контакты; в поле «С чем хотите поработать» есть необязательный визард самоописания. Его служебная подсказка добавляется в заметку заявки (не диагноз) и не включается в Telegram.
- `/cabinet`, `/auth` — кабинет/вход (в sitemap/robots закрыты от индексации)
- legacy-ссылки `?book={slug}` и `/psy/{slug}#book` перенаправляют на страницу записи

## SEO: сайт как портал для специалистов и клиентов

Цель индексации веб-пауками и индексаторами — не «каталог психологов», а
**портал для специалистов (самозанятых, ИП) и клиентов/заказчиков**: витрина
кабинетов, услуги и цены, онлайн-запись, оплата, напоминания. Ключевые слова
разбиты на 4 блока, они стоят в `meta keywords` в `index.html` (в том же
порядке) и продублированы в `title`, `meta description`, Open Graph и
статическом JSON-LD `WebSite` + `ProfessionalService` (script
`id="ld-json-portal"`; динамический JSON-LD профилей — `id="ld-json"` через
`js/services/seoService.js`).

**1. Портал (специалисты + клиенты/заказчики):** портал специалистов, портал
для специалистов и клиентов, онлайн-запись к специалисту, онлайн-запись к
психологу, запись без регистрации, каталог специалистов, каталог услуг,
онлайн-консультация, оплата услуг онлайн, личный кабинет специалиста, услуги
самозанятых, самозанятость, услуги ИП, индивидуальный предприниматель.

**2. Виды деятельности (психология, программирование + другие для самозанятых
и ИП):**

- психология: психолог, психолог онлайн, психологическая помощь, психотерапия,
  психотерапевт, семейный психолог, кризисный психолог, тревога, депрессия,
  стресс, выгорание, самооценка, отношения, развод;
- программирование/IT: программист, разработка сайтов, создание сайта под ключ,
  веб-разработка, веб-разработчик, мобильная разработка, веб-приложение, SPA,
  JavaScript, Node.js, Supabase, PostgreSQL, REST API, Telegram-бот,
  IT-консультант, IT-услуги, автоматизация процессов, MVP-продукт;
- другие типовые виды деятельности самозанятых и ИП: дизайнер (графический
  дизайн, веб-дизайн, UI/UX, логотип, иллюстрация), копирайтинг, SMM,
  таргетированная реклама, SEO-продвижение, интернет-маркетинг, репетитор,
  онлайн-курсы, подготовка к экзаменам, языковые курсы, переводчик,
  юридическая консультация, юрист, регистрация ИП, бухгалтерские услуги,
  ведение бухгалтерии, бухгалтер для ИП, финансовый консультант, коуч
  (бизнес/карьерный), фотограф, видеосъёмка, косметолог, косметология,
  нутрициолог, фитнес-тренер, клининг, ремонт и отделка, автосервис, риелтор,
  организация мероприятий, мебель на заказ, ручная работа.

**3. Специализация Натальи Михайловской** (гештальт-терапевт, кризисный и
семейный психолог, Гродно; источник — nataliamikhailouskaya.by,
`supabase/seed.sql`, публичная карточка `/psy/наталия-михайловская-19`):

> Наталия Михайловская, психолог Михайловская Гродно, гештальт-терапевт,
> гештальт-подход, гештальт-терапия, кризисные ситуации, работа с травмой,
> семейная терапия, системная семейная психотерапия, супружеское
> консультирование, семейное консультирование, психолог в Гродно,
> психолог Гродно, онлайн-психолог (Skype, WhatsApp, Viber, Telegram, Zoom),
> тревожность, страхи, неуверенность в себе, низкая самооценка, поиск себя,
> личные границы, чувство вины, стыд, обида, злость, созависимые отношения,
> переживания измены, болезненное расставание, развод, стресс, эмоциональное
> выгорание, упадок сил, НЛП, НЛП-практик, ИНП, метафорические ассоциативные
> карты, МАК, психолог с опытом 10 лет.

**4. Специализация Дмитрия (автор портала, веб-разработчик):**

> Дмитрий, разработчик сайтов на заказ, веб-разработчик, фронтенд-разработчик,
> JavaScript-разработка, SPA-приложения, система онлайн-записи, календарь
> записи, Telegram-уведомления, оплата онлайн, разработка кабинета
> специалиста, сайт для самозанятых, сайт для ИП, веб-услуги для малого
> бизнеса, IT-фриланс.

Правила расширения:

- новая профессия в каталоге — добавить её в `Professions`
  (`js/models/entities.js`), тогда SEO-слой (`seoService.js`) автоматически
  подберёт schema.org-тип для её карточки;
- новый специалист — ключевые слова его специализации берутся из его профиля
  в БД (`directions`, `specialization`), как у Натальи; свой блок можно
  держать в `meta keywords` рядом с блоком «Дмитрий»;
- перенос/изменение ключевых слов — только в `index.html` (статика) и в этом
  разделе; runtime-метки маршрутов меняет `seoService.js`, не править их
  руками.

## Кабинет психолога

- **Книга записей** — журнал всех записей: фильтры (предстоящие/ждут действия/прошедшие), подтверждение, перенос, заметка о сессии, неявка, «В календарь»
- **Расписание** + **Занятость** (блокировки, Google Calendar iCal)
- **Клиенты** — карточка клиента: сессии + записи о работе (журнал, виден только психологу)
- **Задачи** — дела кабинета с сроками и привязкой к клиенту
- **Блокнот** — планировщик: заметки с датой, закрепление
- **Профиль** — всё публичное содержание страницы + реквизиты (налоговые требования)

Залогиненный психолог на своей странице `/psy/{slug}` видит зелёную панель
«Это ваша публичная страница» с быстрыми переходами (Книга записей, Задачи, Блокнот, Кабинет).

## Занятость

Кабинет → «Занятость»: блокировки (выходной/занят/отпуск, весь день или интервал) закрывают слоты онлайн-записи. Google Calendar: секретный iCal-адрес импортируется в блокировки; сессии добавляются в календарь кнопкой «В календарь» (шаблонная ссылка Google, как у Calendly/Booksy).

## Данные: строго серверные

Каталог/профили/услуги/занятость загружаются только из Supabase (`loadServerCatalog`).
Если сервер недоступен или не заполнен — портал показывает причину и инструкцию
(локальный seed **не** подставляется). Демо-данные включаются только явной кнопкой
«Показать демо-данные» и помечаются жёлтой плашкой «ДЕМО».

Подключение БД: Supabase → SQL Editor → выполнить `supabase/schema.sql`, затем
`supabase/seed.sql` → в приложении нажать «Повторить» (бейдж в шапке станет
«Данные: сервер»). Роадмап и регламент подключения LLM-клиентов и Supabase MCP
Server см. в `docs/ROADMAP-DATABASE-CONNECTION.md`.

## Вход психолога — Google OAuth (Supabase Auth)

Единственный путь (issue #88):

```text
Google OAuth → Supabase Auth → auth.uid() → свой кабинет
```

На `#/auth` — кнопка «Войти через Google» (PKCE). Возврат на
`https://a1dmitry.github.io/Psihologist-cabinet/` без hash. Сервер
(`link_or_create_psychologist_for_google`) берёт email из `auth.users`,
привязывает свободную запись или создаёт одну новую. Повторный вход не
дублирует профиль. Отключённый кабинет входом не реактивируется.

Email/OTP/`auth-code` для психолога из UI сняты. Клиентский Google при
записи (опрос, #41) — отдельный контур, не вход в кабинет.

Настройка (владелец, секреты не в репозиторий):
1. Google Cloud → Authorized redirect URI:
   `https://phiavtroybgwyjdhqqkh.supabase.co/auth/v1/callback`
2. Supabase → Authentication → Providers → Google: включить, Client ID/Secret.
3. Site URL / Redirect URLs = `https://a1dmitry.github.io/Psihologist-cabinet/`
4. SQL Editor: `supabase/migrations/20260925_google_specialist_signup.sql`

Подробности: `docs/GOOGLE-SPECIALIST-SIGNUP.md`. Смоук UI:
`node tests/auth-ui-pending.mjs`, `node tests/google-oauth-client.mjs`.

## Карточка специалиста — вся модель сайта через БД

На `/psy/{slug}` отображается полный профиль из БД: фото (по умолчанию — с сайта-
первоисточника, иначе инициалы), приветствие/подход/о себе, направления, услуги и
цены, образование, опыт, кнопки оплаты + реквизиты, **адрес практики со картой
проезда** (Google-embed + маршруты Google/Яндекс) и **контакты с мессенджерами**
(телефон, WhatsApp `wa.me`, Viber `viber://chat`, Telegram, email, соцсети) — как
принято на сайтах специалистов. Внизу — ссылка на первоисточник данных.

Смоук карточки: `node verify_profile.mjs` (29 проверок отрисовки модели сайта из
серверных строк БД).

## Онлайн-запись — 3 шага, без регистрации

`/book/{slug}`: **Услуга → Время → Контакт**.

- Индикатор прогресса; «Назад» не теряет введённые данные, «Далее» недоступна,
  пока шаг не заполнен (и понятно подписано, чего не хватает).
- **Клик по услуге — сразу её свободные окна** (с профиля: `/book/{slug}?service=<id>`).
- Сетка слотов считается по длительности услуги: 90-минутная услуга не предложит
  18:00 при приёме до 19:00 и не пересечёт чужую запись. Занятые часы зачёркнуты,
  причина — в подсказке.
- **Часовой пояс клиента**: слоты показываются и в поясе специалиста, и в вашем
  (`Intl`-определение пояса, корректный переход через DST и границы суток).
  В БД пишется время специалиста; пояс клиента — в `sessions.client_timezone`
  + `client_utc_offset_min` (SR-001, в main; в проде — после re-apply `schema.sql`).
- Оплата/аванс/резерв — прежняя политика кабинета; статусы звучат по-человечески
  («ждём подтверждения», «слот зарезервирован до …»).

Смоук клиентского пути: `node tests/booking-wizard.mjs` (шаги, слоты 90 мин,
часовые пояса, ссылка «услуга → окна»).

## Telegram-уведомления (свой бот психолога)

Вкладка «Уведомления» в кабинете: токен от @BotFather → «Проверить», «Найти чат»
(getUpdates), переключатели событий (новая запись / напоминания клиенту / оплаты),
«Проверить подключения (/start)». Клиент подключает напоминания по ссылке-приглашению
`https://t.me/<бот>?start=<clientId>` (карточка клиента → «Как подключить»).
Механика: напоминания и outbox новых записей уходят, пока кабинет открыт (таймер 60 с,
watermark `last_notified_session_at` от дублей); для мгновенной доставки с публичной
страницы — задать `NOTIFY_WEBHOOK_URL` в `js/services/supabaseConfig.js` и задеплоить
`supabase/functions/telegram-notify` (токен живёт только в Edge Function).
Смоук: `node verify_telegram.mjs` (57 проверок).

## Кабинет через сервер (без заглушек)

После входа все данные кабинета тянутся с сервера по RLS-владельцу и каждая мутация
сразу пишется в БД (write-through): клиенты (привязочные строки; PII — только в
шифрованном сейфе на устройстве), сессии (создание/перенос/подтверждение/неявка/оплата/удаление),
настройки (оплата, слоты, Google Calendar), блокировки занятости, услуги, задачи,
блокнот, записи о клиентах, лист ожидания. Локальное — только зеркало для рендера.

Полный исходник также в артефактах проекта (`portal/`).
