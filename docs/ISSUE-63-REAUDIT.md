# ISSUE-63-REAUDIT — Main Re-Audit мобильного кабинета на актуальном `main` + устранённые дефекты

> **АУДИТОР: САМ** для собственных исправлений (тот же конвейер: исполнитель
> `EXEC-rzVmS30TVl`, ветка `arena/01a0de80-psihologist-cabinet`, сессия Arena Agent Mode).
> **АУДИТОР: НЕЗАВИСИМЫЙ (другой исполнитель)** для ре-аудита чужой реализации:
> продюсеры #63 — `EXEC-ZLCS0Evg7b` (PR #101) и `EXEC-aU56e2ldLr` (PR #103, Challenger);
> эта сессия к их коду отношения не имеет. Формально по §6.6 Challenger собственных
> правок остаётся **незакрытым** — см. §9/§11.
> **Дата:** 2026-09-26 UTC. **Базовый SHA:** `c6c2f2e7aba7e886d5c0efd6ed40594ddbaaca4b`
> (`origin/main`, merge PR #103). **Окружение:** node v22.22.3, python 3.11.2,
> embedded-postgres PG 18.4 (наборы схемы), `tailwindcss@3.4.17`.
> **Production/браузер:** egress из песочницы до `*.supabase.co` закрыт (curl → 000),
> headless-браузер недоступен (apt/CDN → 000) — попиксельный мобильный рендер
> не измерялся, это граница #63 (подтверждение владельцем на устройстве).

---

## 0. Зачем этот документ (Fresh State, §6.15)

Предыдущий Main Re-Audit #63 (`docs/MAIN-AUDIT-63.md`) выполнен на `8c96311`
(PR #101). После него в `main` влит PR #103 (`c6c2f2e`), который менял
`index.html`, `js/app.js`, `js/services/seoService.js`, `privacy.html`,
`verify_pages.py` — то есть **контур #63 изменился после проверки**.
По §6.15 тест старого SHA не сертифицирует новый SHA: выполнен повторный
ре-аудит на актуальном дереве и найдены два дефекта мобильной раскладки,
которые не ловили ни 59 проверок набора `tests/mobile-cabinet.mjs`,
ни Challenger-аудит `docs/ISSUE-63-CHALLENGER.md`.

---

## 1. ПЕРВОНАЧАЛЬНАЯ ЦЕЛЬ

Issue #63 (MX-01/MX-02/MX-04, F1): мобильный кабинет 1.0 — bottom tab bar
(4 раздела + «Ещё» со всеми остальными, все 15 разделов со смартфона),
safe-area, бейдж «Ожидание», десктопный сайдбар без деградации; строки
главной/расписания/журнала — карточный паттерн (primary + меню «…», цели тапа
≥44×44px, деструктивные действия отделены и подтверждаются UI-диалогом);
тексты и структура десктопа не меняются. Границы: схема БД/RPC/Edge Functions
не трогаются; «готово» — после `verify_pages.py` против живого `devserver.py`
и зелёного `npm run verify`.

Цель этой сессии (без добавок): закрыть остаток #63 — Main Re-Audit на
актуальном `main` и подтверждение владельцем, — не расширяя скоуп.

## 2. РЕЗУЛЬТАТ

Ре-аудит выполнен; найдено **3 дефекта** в контуре #63 (2 функциональных
в мобильной раскладке, 1 гигиена кода), все три устранены канонически,
регресс-набор дополнен **6 проверками** (fail-before-fix показан):

| # | Дефект | Класс | Фикс |
|---|--------|-------|------|
| D1 | Панель «…» остаётся открытой после тапа по пункту, который не перерисовывает список (ссылка «В календарь»; отмена `uiConfirm`/`uiPrompt` у «Удалить»/«Неявка»/«+ запись о сессии») — деструктивный пункт продолжает висеть поверх строки | функциональный, MX-04 | `js/views/cabinetMobile.js`: любой тап, не являющийся переключением панели и не выбором раздела в sheet, закрывает открытые панели (ветка `if (!e.target.closest('.cab-menu'))` заменена на безусловное `closeAllCabMenus()`) |
| D2 | `.cab-menu { z-index: 30 }` ниже фиксированного таб-бара `.cab-tabbar { z-index: 40 }` (и `.book-cta`, 40): у строк в нижней части экрана панель открывается **под** таб-баром, последние пункты («Удалить», «Неявка») накрыты и тап уходит в навигацию | функциональный, MX-04/MX-02 | `css/tailwind.src.css`: `z-index: 45` (выше таб-бара/CTA, ниже модалок и sheet `z-50`) + пересборка `css/tailwind.css` |
| D3 | В комментарии `toggleCabMenu` — постороннее имя человека («id записей Oleksandr Dermansky») и ложная посылка: id генерируются как `${prefix}_${Date.now().toString(36)}_${random}` (`js/core/dbContext.js:15`), невалидных для селектора символов там не бывает | гигиена/достоверность комментария | комментарий переписан по факту (генератор id + try/catch как вторая линия защиты), имя убрано |

Изменённые файлы: `js/views/cabinetMobile.js`, `css/tailwind.src.css`,
`css/tailwind.css` (пересборка), `tests/mobile-cabinet.mjs`, плюс этот документ,
`docs/RRSI-CACHE.md` (урок), `docs/CURRENT-STATE.md` (строка #63),
`.claims/issue-63.EXEC-rzVmS30TVl.json` (claim).

## 3. ПРОВЕРКА

Порядок — канонический (сначала красный тест, потом фикс):

1. **Baseline на `c6c2f2e` (до правок):** `npm run verify` → **40/40 наборов,
   1803 проверки, exit 0**; `BASE_URL=http://127.0.0.1:8765 python3 verify_pages.py`
   против живого `python3 devserver.py 8765` → **ALL PASS (13/13)**.
2. **Fail-before-fix (негативный контроль, §6.5):** после добавления 6 проверок
   и до фикса — `node tests/mobile-cabinet.mjs` → **exit 1, 4 FAIL / 61 PASS**:
   ```text
   FAIL  MX-04: панель «…» выше фиксированного таб-бара и sticky-CTA (z-index, исходник) → src: .cab-menu=30, .cab-tabbar=40, .book-cta=40
   FAIL  MX-04: панель «…» выше таб-бара и в СОБРАННОМ css/tailwind.css (его отдаёт сайт/превью) → css: .cab-menu=30, .cab-tabbar=40 — сборка не обновлена из css/tailwind.src.css
   FAIL  Поведение: тап по пункту меню без перерендера («В календарь») закрывает панель → предусловие open=true; после тапа open=true, aria-expanded=true
   FAIL  Поведение: тап по деструктивному пункту закрывает панель (отмена диалога не оставляет «Удалить» на виду) → предусловие open=true; после тапа open=true
   ```
3. **После фикса:** `node tests/mobile-cabinet.mjs` → **ALL PASS, 65 проверок, exit 0**;
   `npm run verify` → **40/40 наборов, 1809 проверок (+6), exit 0**;
   `verify_pages.py` против живого devserver → **ALL PASS (13/13)**.
4. **Пересборка CSS тем же инструментом, что и CI** (`npx tailwindcss@3.4.17`,
   `pages.yml`): байтовое сравнение `git show HEAD:css/tailwind.css` с новой сборкой —
   единственное отличие `30` → `45` (длина файла 30095 → 30095). Проверено, что
   devserver отдаёт уже собранный файл с `z-index:45` (curl по живому превью).
5. **Ground truth после каждой правки** (урок реестра RRSI от 2026-09-26, #63):
   `node --check js/views/cabinetMobile.js`, построчный `git diff` всех четырёх
   файлов, повторный прогон набора — «success» инструмента правок доказательством
   не считался.
6. **Смоук превью:** devserver поднят на `0.0.0.0:8765` (живое превью сессии),
   маршруты `/`, `/cabinet`, `/psy/<slug>`, `/book/<slug>`, `/auth`, `/onboarding`,
   `/booking-done`, `/reply` — 200, отсутствующий ассет — 404.

## 4. ПОЛОЖИТЕЛЬНЫЕ РЕЗУЛЬТАТЫ

- DoD #63 на актуальном дереве подтверждён чтением кода/разметки и тестами:
  таб-бар = 4 раздела + «Ещё»; sheet = 11 разделов; `таб-бар ∪ sheet` = 15 разделов
  сайдбара (множества равны, порядок и названия сайдбара не тронуты);
  `.cab-tabbar` — fixed + `env(safe-area-inset-bottom)`; `.cab-content` —
  `padding-bottom: 5.5rem` только в `max-width: 767px`; бейдж `.wait-badge`
  в сайдбаре и на «Ещё»; десктоп — `display: contents` на обёртках и
  `cab-ord-1/2/3` (порядок кнопок прежний); деструктивные — `.cab-menu-danger`
  и подтверждение каноном `uiConfirm` (нативных `confirm()/prompt()` в контуре нет).
- Оба функциональных дефекта закрыты с негативным контролем: проверки красные
  на до-фиксовом дереве и зелёные после (пин против возврата).
- Проверка «собственный тап по тогглу по-прежнему ОТКРЫВАЕТ панель» добавлена
  как страховка от чрезмерного закрытия (фикс D1 не сломал открытие меню).
- Полный гейт зелёный (40/40, 1809 проверок, включая наборы на настоящем
  PostgreSQL 18.4) и смоук маршрутов против живого devserver — ALL PASS.
- Изменение presentation-only: схема БД, RPC, Edge Functions, `supabase/**`
  не тронуты; канон availability/booking не переопределялся (§6.14).

## 5. ОТРИЦАТЕЛЬНЫЕ РЕЗУЛЬТАТЫ (ограничения)

- **Мобильный рендер на устройстве не подтверждён** (граница #63): в песочнице
  нет ни эмулятора, ни headless-браузера (apt и CDN Playwright/Puppeteer → 000),
  поэтому z-порядок и геометрия проверены статически (CSS-правила + тесты),
  а не попиксельно.
- **Превью сессии не показывает кабинет авторизованным:** вход специалиста —
  Google OAuth через Supabase Auth, а redirect-allowlist настроен на origin
  Pages (`https://a1dmitry.github.io`), не на origin превью песочницы. Публичная
  часть (портал, каталог, `/psy/<slug>`, `/book/<slug>`) в превью работает —
  данные берёт браузер владельца напрямую из Supabase; кабинет — нет
  (`js/app.js:617 if (!authService.isAuthenticated())`).
- **GitHub-доступ оборвался в середине сессии и восстановлен позже:** `GH_TOKEN` →
  `Bad credentials` (HTTP 401) в окне 17:22:52Z–18:11Z; в этом окне push/PR/комментарий
  были невозможны и claim взят с `--offline-ok`. После восстановления (18:11Z)
  ветка запушена и открыт **PR #104**: CI `verify` — SUCCESS, `probe` — SUCCESS
  на SHA `1b81b2d2` (внешний канал). **CLAIM-маркер в Issue по-прежнему не
  публикуется**: у интеграции нет `issues:write` (`Resource not accessible by
  integration`) — владение видно только по карточке в ветке (риск двойного взятия).
- **Независимый Challenger собственных правок не выполнен** (§6.6) — я их автор;
  независимость есть только по отношению к коду продюсеров #63.
- Production-состояние не перепроверялось (egress закрыт); последний внешний
  LIVE-срез — `Production read-only probe`, run 36254754065, 2026-09-26T16:14:59Z,
  SHA `6429a070` (до этого ре-аудита).

## 6. ТУФТА НАЙДЕНА

- **В чужом контуре (найдено ре-аудитом):** статус «Challenger: COMPLETED / PASS»
  в `docs/ISSUE-63-CHALLENGER.md` при двух живых дефектах мобильной раскладки
  (D1, D2) — форма 6 (самосертификация): аудит был помечен `АУДИТОР: САМ`
  и проводился той же сессией, что и правки, без контрпримера «тест зелёный,
  а задача не решена» (§6.3). Формально маркер был честный, но вывод
  «Дефектов и регрессий не обнаружено» — неверен.
- **В своих проверках (найдено и исправлено до коммита):** первая версия нового
  поведенческого теста давала **ложное зелёное** — `toggleCabMenu` вызывался
  без явного предусловия, и на уже открытой панели он её закрывал: проверка
  «тап по деструктивному пункту закрывает панель» проходила, хотя тапа
  фактически не было (3 FAIL вместо 4 на до-фиксовом дереве). Исправлено:
  предусловие выставляется явно (`closeAllCabMenus()` → `toggleCabMenu()` →
  assert `opened === true`) и входит в условие проверки.
- **Ложная посылка в комментарии (D3):** постороннее имя человека и утверждение
  о «невалидных символах в id записей», которого в коде нет (id генерируются
  base36) — имитация обоснования.
- Метрической подгонки, Cargo Cult и сломанного превью нет: превью поднято
  на реальном `devserver.py` (0.0.0.0:8765), смоук прогнан против него,
  числа проверок приведены по фактическому выводу гейта.

## 7. КОРЕННЫЕ ПРИЧИНЫ (5 Why)

**D1 (панель не закрывается):**
1. Почему панель оставалась открытой? — обработчик закрывал меню только при тапе **вне** `.cab-menu`.
2. Почему так написано? — рассчитывали, что действие пункта всегда вызывает перерендер списка, который уберёт панель.
3. Почему расчёт неверен? — есть действия без перерендера: `<a href target="_blank">«В календарь»` и отмена `uiConfirm`/`uiPrompt` (`if (…await uiConfirm(…))` — ветка «отмена» не рендерит ничего).
4. Почему это не поймали тесты? — набор проверял «тап вне закрывает» и «перерендер убирает панель», но не «тап по пункту закрывает»; инвариант «меню закрыто после активации пункта» не был сформулирован.
5. Корень: **инвариант жизненного цикла всплывающего меню сформулирован по одному сценарию (тап вне) вместо полного набора сценариев закрытия** — тот же класс, что RRSI-кандидат от 2026-09-26 по #64 («UI-инвариант = состояние модели + состояние элемента управления в рендере»).

**D2 (панель под таб-баром):**
1. Почему пункты оказались недоступны? — панель рисовалась под фиксированным таб-баром и перехватывала тап.
2. Почему? — `.cab-menu { z-index: 30 }` при `.cab-tabbar { z-index: 40 }`.
3. Почему значения разошлись? — z-index таб-бара добавляли в одном файле/задаче (MX-02), z-index панели — в другом месте той же задачи, без единой шкалы слоёв.
4. Почему не поймал контроль? — проверки читали каждое правило по отдельности («панель скрыта/открывается классом», «таб-бар прижат к низу»), сравнения слоёв не было.
5. Корень: **нет канонической шкалы z-слоёв мобильного кабинета и проверки относительного порядка** (контент < таб-бар/CTA < всплывающие меню < модалки/sheet).

**D3:** комментарий писался «под защиту» без проверки факта (как генерируются id) — корень: комментарий как оправдание кода, а не как описание факта.

## 8. РЕМОНТ (системный, а не точечный)

- **D1:** закрытие меню выведено из перечисления сценариев в один инвариант:
  «любой тап, который не переключает панель и не выбирает раздел sheet,
  закрывает открытые панели». Новый сценарий закрытия не требует правки
  обработчика (DRY: одно правило вместо списка исключений).
- **D2:** шкала z-слоёв зафиксирована в комментарии правила и **закреплена
  тестом** относительного порядка: `.cab-menu > .cab-tabbar`, `.cab-menu > .book-cta`,
  `.cab-menu < 50` (модалки/sheet). Проверка читает **и исходник, и собранную
  сборку** — рассинхрон `tailwind.src.css` ↔ `tailwind.css` (когда сайт/превью
  отдаёт старую сборку) становится красным, а не тихим.
- **D3:** комментарий описывает факт (генератор id) и роль try/catch (вторая линия).
- **Poka-Yoke против ложного зелёного в новых тестах:** предусловие каждой
  поведенческой проверки входит в её условие (`openedLink === true && …`).
- **Kaizen:** урок занесён в реестр `docs/RRSI-CACHE.md` (сигнал 2026-09-26,
  #63 ре-аудит) — «инвариант всплывающего элемента = полный набор сценариев
  закрытия + относительный z-порядок против фиксированной навигации, проверенный
  и в исходнике, и в артефакте сборки».

## 9. ОСТАВШИЕСЯ РИСКИ

1. **Подтверждение на устройстве не получено** — главный остаток #63
   (граница Issue: «мобильный рендер владельцем не подтверждён»). Без него
   статус не выше ЧАСТИЧНО ЗАВЕРШЁННОГО.
2. **Независимый Challenger этих правок не проведён** (§6.6) — нужен другой
   исполнитель/владелец; внешний CI-гейт на PR не запущен (нет доступа к GitHub).
3. **CLAIM-маркер не опубликован** (нет `issues:write` у интеграции): другие
   исполнители видят #63 как FREE и могут взять его параллельно — тот же сигнал
   реестра, что по #65 (двойное решение). Мера: карточка `.claims/issue-63.EXEC-rzVmS30TVl.json`
   запушена в `origin/arena/01a0de80-psihologist-cabinet` + PR #104 открыт
   (владение видно по PR); полная мера — право `issues:write` исполнителю.
4. **Найдено вне скоупа #63, не исправлялось (scope-дисциплина §4):**
   - на мобильном **нет выхода из кабинета и нет блока аккаунта**: `#btn-logout`
     и `#cab-account` живут только в сайдбаре `hidden md:flex` (`index.html:259, 284–288`),
     мобильный хедер даёт только «Портал» (`index.html:294–297`). Психолог со
     смартфона не может разлогиниться — это касается канона входа #40
     («Logout/reload/session restoration use the same Supabase Auth session»).
     Кандидат в отдельный Issue (P1/P2, mobile auth-session control);
     в #63 не входит: DoD перечисляет 15 разделов, а не действия сессии.
   - карточный паттерн MX-04 применён к строкам главной/расписания/журнала
     (как и предписано Issue); списки клиентов/задач/услуг/ожидания остались
     прежними — наблюдение, не дефект #63.
   - LIVE-срез 2026-09-26T16:14:59Z (внешний канал, run 36254754065):
     `create_booking` для anon в production отсутствует (PGRST202), снятая
     `auth-code` отвечает в проде (`REVIVED_RETIRED_PATH`), `external_google=true`.
     К #63 отношения не имеет (контуры #35/#40/#46), зафиксировано как наблюдение.

## 10. ДОКАЗАТЕЛЬСТВО

```text
BASELINE SHA:  c6c2f2e7aba7e886d5c0efd6ed40594ddbaaca4b (origin/main, merge PR #103)
CHECKED SHA:   то же дерево + правки этой сессии (ветка arena/01a0de80-psihologist-cabinet)
ОКРУЖЕНИЕ:     песочница Arena (node v22.22.3, python 3.11.2, PG 18.4 embedded),
               egress до *.supabase.co закрыт (000), headless-браузер недоступен
ДАТА/UTC:      2026-09-26, 16:17Z–17:5xZ

1. Гейт до правок:            npm run verify → 40/40 наборов, 1803 проверки, exit 0
2. Смоук до правок:           verify_pages.py против devserver.py 8765 → ALL PASS 13/13
3. Fail-before-fix:           node tests/mobile-cabinet.mjs → exit 1, 4 FAIL / 61 PASS (лог §3.2)
4. После фикса (набор):       node tests/mobile-cabinet.mjs → ALL PASS, 65 проверок, exit 0
5. После фикса (гейт):        npm run verify → 40/40 наборов, 1809 проверок, exit 0
6. После фикса (смоук):       verify_pages.py против живого devserver → ALL PASS 13/13
7. Артефакт сборки:           npx tailwindcss@3.4.17; diff HEAD→new = '30'→'45' (единственное отличие)
8. Отдача превью:             curl http://127.0.0.1:8765/css/tailwind.css → .cab-menu{…z-index:45}
                              curl http://127.0.0.1:8765/js/views/cabinetMobile.js → closeAllCabMenus() ×3
9. Claim:                     .claims/issue-63.EXEC-rzVmS30TVl.json (active до 18:53:10Z,
                              перехват истёкшего claim EXEC-aU56e2ldLr — TTL истёк 17:22:18Z,
                              его работа слита: PR #101 реализация, PR #103 Challenger)
10. GitHub-канал:             401 Bad credentials в окне 17:22:52Z–18:11Z (push/PR были невозможны);
                              после восстановления — push OK, PR #104 (OPEN, MERGEABLE/CLEAN)
10a. Внешний CI (АУДИТОР: ВНЕШНИЙ — GitHub Actions): verify SUCCESS (35s), probe SUCCESS (21s)
                              на SHA 1b81b2d2; probe-срез прода 27/27 измерено, drift 3
                              (auth-code REVIVED, create_booking missing ×2) — к #63 не относится
10b. CLAIM-маркер в Issue:    НЕ опубликован — issues:write у интеграции нет
                              (Resource not accessible by integration); карточка в ветке запушена
11. Production:               не перепроверялось из песочницы; внешний срез — probe run 36254754065 (16:14:59Z)
```

## 11. ОКОНЧАТЕЛЬНЫЙ СТАТУС

**ЧАСТИЧНО ЗАВЕРШЕНО.**

- Ре-аудит #63 на актуальном `main` выполнен, 3 дефекта устранены канонически,
  негативный контроль (fail-before-fix) в гейте, гейт 40/40 и смоук 13/13 зелёные.
- Не закрыто: (а) подтверждение мобильного рендера владельцем на устройстве —
  граница #63; (б) независимый Challenger собственных правок — внешний CI-гейт
  на PR #104 пройден (`verify` SUCCESS, `probe` SUCCESS, SHA `1b81b2d2`), но это
  автоматический канал, а не независимый проверяющий по §6.6; (в) CLAIM/отчёт
  в Issue не публикуются (нет `issues:write`).
- **(г) merge выполнен владельцем/агентом по его указанию:** PR #104 → `main @
  f087642b7cea347eeab1cd17a7a9dfbf20467cc6` (2026-09-26T18:25:29Z). По §6.7
  merge — начало проверки: Main Re-Audit на фактическом merge-SHA выполнен
  (`docs/MAIN-AUDIT-63B.md`) — фиксы присутствуют в merged-дереве, гейт на
  пристальном снимке 40/40 (1809), смоук 13/13, внешний CI-деплой Pages success
  вместе с шагом `Smoke check deployed site`. Закрытие Issue #63 — за владельцем
  после прогона мобильной раскладки на устройстве.
- Основание не объявлять «ЗАВЕРШЁННЫЙ»: §3 (запрет самосертификации) и §6.15
  (Fresh State) — попиксельного мобильного рендера и независимой проверки нет.

## 12. УВЕРЕННОСТЬ

**Высокая** — для фактов репозитория: дефекты D1/D2 воспроизведены на до-фиксовом
дереве красными проверками (а не «на глаз»), фиксы подтверждены зелёным набором
(65 проверок), полным гейтом (40/40, 1809) и смоуком маршрутов против живого
`devserver.py`; артефакт сборки сверен байтово. Это не самовнушение: каждое
утверждение §10 повторяемо одной командой из этого файла.

**Ограниченная** — для визуального результата на реальном устройстве: z-порядок
и геометрия выведены из CSS-правил и проверены статически, headless-браузера
в песочнице нет. Ровно поэтому статус — ЧАСТИЧНО ЗАВЕРШЕНО, а закрытие #63
выносится на подтверждение владельца (прогон на смартфоне: превью сессии,
локальный `python3 devserver.py 8765` + вход, или задеплоенный Pages после merge).

---

## QA-ПРОГОН (роль QA/Challenger, 2026-09-26 18:11Z–18:25Z)

> **АУДИТОР: САМ** для собственных правок (тот же исполнитель `EXEC-rzVmS30TVl` —
> независимого Challenger по §6.6 нет, это зафиксировано, а не замаскировано).
> **АУДИТОР: ВНЕШНИЙ** для CI-канала: GitHub Actions `Quality Gate` (`verify`)
> и `Production read-only probe` (`probe`) на PR #104, SHA `1b81b2d2`,
> 2026-09-26T18:13Z — оба SUCCESS, probe измерил прод 27/27 (недоступно 0).
> Метод QA: **не** повторять тесты продюсера, а пересчитать факты заново —
> отдельный харнес, реконструкция baseline, мутационная матрица.

### A. Независимый QA-харнес (свой DOM-стаб, свои утверждения из текста DoD)

Прогон `/tmp/qa-harness.mjs` (в репозиторий не коммитится — это измерительный
стенд QA, не продукт): **29/29 PASS, exit 0**. Покрыто: 10 сценариев жизненного
цикла панели/sheet (включая оба исправленных), 4 CSS-инварианта (z-порядок,
safe-area, ≥44px, десктопный `display:contents`), 4 пересчёта DoD из разметки
(15 разделов, множества таб-бар ∪ sheet, 5 разделов MX-01, бейджи), 4 регресс-факта
по `git diff` к baseline, 3 проверки шаблонов MX-04 и канона подтверждений.

Дефект самого стенда, найденный и исправленный в процессе: первая версия
имитировала клик только на `document`, из-за чего «кнопка «Ещё» открывает sheet»
дала ложный FAIL (в браузере клик вызывает слушатель узла и всплывает до
`document`). После починки модели клика — 29/29. Продуктового дефекта нет.

### B. Реконструкция baseline и мутационная матрица (есть ли зубы у регресс-набора)

| Прогон | Дерево | Набор | Результат | Вывод |
|---|---|---|---|---|
| B0 | `c6c2f2e` (baseline) | штатный (baseline) | exit 0, **59 PASS** | baseline зелёный; счётчик «59 → 65» в отчёте подтверждён пересчётом |
| B1 | `c6c2f2e` (baseline) | новый (65 проверок) | exit 1, **4 FAIL / 61 PASS** | fail-before-fix воспроизведён **независимо** (на чистом baseline-дереве) |
| M1 | HEAD, `z-index:45→30` в src И в сборке | новый | exit 1, **2 FAIL** | мутант убит: откат фикса D2 красит гейт |
| M2 | HEAD, src=45, сборка=30 (рассинхрон) | новый | exit 1, **1 FAIL** | мутант убит: «починил исходник, забыл пересобрать» больше не проходит |
| M3 | HEAD, возврат `if (!e.target.closest('.cab-menu'))` | новый | exit 1, **2 FAIL** | мутант убит: откат фикса D1 красит гейт |
| M4 | HEAD, `willOpen=false` (панель не открывается) | новый | exit 1, **6 FAIL** | мутант убит: предусловие в условии проверки не даёт ложного зелёного |

Ошибка первого прогона матрицы (найдена QA, исправлена): B1 был собран из
`git archive HEAD` вместо `c6c2f2e`, из-за чего «fail-before-fix» показал ложные
65 PASS. Пересчитано на настоящем baseline — таблица выше.

### C. Дефекты окружения/процесса, найденные QA (не продуктовые)

1. **Среда откатила `HEAD` к `c6c2f2e` между ходами**: коммиты `41749db`/`6a47496`
   исчезли из истории ветки, правки остались незакоммиченными. Восстановлено
   повторным коммитом `4213724` (+ `1b81b2d` — карточка claim) и немедленным push.
   Урок: пушить сразу, «автосохранение» не сохраняет историю git.
2. **`node_modules` стёрт тем же restore** → локальный гейт дал 8 падений
   `ERR_MODULE_NOT_FOUND: embedded-postgres` (1513 проверок вместо 1809).
   Это отказ окружения, не продукта: после `npm install` гейт **40/40, 1809, exit 0**;
   независимо — CI `verify` SUCCESS на том же SHA.
3. **Процесс превью не пережил границу хода** (curl → 000) → поднят заново
   (`0.0.0.0:8765`), смоук `verify_pages.py` против него — **ALL PASS 13/13**.
4. **Окно `GH_TOKEN` 401** (17:22:52Z–18:11Z) — см. §5/§10; после восстановления
   выполнены push, PR #104, claim-refresh. CLAIM-комментарий по-прежнему не
   публикуется (`issues:write` нет).

### D. Сводка QA-вердикта по контуру #63

- Продуктовые дефекты после фикса: **не найдены** (29/29 независимый харнес,
  65/65 штатный набор, 40/40 гейт, 13/13 смоук, CI verify/probe SUCCESS).
- Все 4 мутанта убиты → регресс-набор действительно держит оба инварианта.
- Незакрытое по существу: рендер на устройстве (владелец) и независимый
  Challenger (§6.6) — без них статус не поднимается выше ЧАСТИЧНО ЗАВЕРШЕНО.
