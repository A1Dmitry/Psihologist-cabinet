# ISSUE-34 — финальный отчёт независимого Challenger (12 разделов по RULES §5)

**АУДИТОР: ВНЕШНИЙ** — относительно Producer PR #32: отдельный агентный процесс
(Arena Agent Mode, ветка `arena/01a0d3a7-psihologist-cabinet`), инструменты:
git/gh api (GitHub), node v22.22.3 + embedded-postgres PG 18.4 (локально),
curl (доказательство блокировки egress), python3. Дата всех проверок: **2026-09-24 UTC**.
Проверенный SHA: `4d490d2efb6f5aa88604c2b7e2ae9296dd57785a` (актуальный
origin/main после merge PR #32; из origin получен и проверен повторно в конце
аудита — не сдвинулся). Дерево: `git write-tree == HEAD^{tree}` — чистое.

Чужие доказательства маркируются: **[А-пакет]** = live-пробы предыдущей сессии
(АУДИТОР: САМ того конвейера; мне не подконтрольны, используются как заявленные,
не как доказанные). Отчёт D1 (`docs/D1-REPORT.md`) использован ТОЛЬКО как карта
утверждений под атаку, не как доказательство.

Карта доказательств — `docs/ISSUE-34-TRIAGE.md` (обновление 2026-09-24, эта сессия).

---

## 1. ПЕРВОНАЧАЛЬНАЯ ЦЕЛЬ

Issue #34 (создано 2026-09-24): «Восстановить закрывающий контур Quality Gate
после PR #32 и получить независимое доказательство текущего состояния `main` и
production» — независимый Challenger на main @ 4d490d2e: baseline (TASK 1),
Challenger-аудит D1 (TASK 2), production create_booking (TASK 3), production
registration E2E (TASK 4), production auth-code (TASK 5), security/privacy
negatives (TASK 6), матрица claims (TASK 7), issue #21 отдельно (TASK 8),
schema drift (TASK 9), Main Re-Audit (TASK 10), Producer/Challenger separation
(TASK 11), итоговый Quality Gate (TASK 12). Результат — доказательная карта +
дочерние issue на каждый ремонт + один конечный статус. Без добавок.

## 2. РЕЗУЛЬТАТ

Фактически получено:

1. **Baseline зафиксирован и воспроизведён:** origin/main = 4d490d2e (не
   ушёл), дерево чистое, окружение записано, гейт перегнан на этом SHA.
2. **Независимый deep Challenger-аудит D1 (TASK 2) выполнен атаками:**
   мутантный анализ (fail-before-fix воспроизведён), негативные контроли exit-кодов,
   чтение серверного enforcement целиком, grep-аудит DRY/семантического
   дублирования, ручная сверка семантик engine↔SQL.
3. **Найдены и оформлены 2 дочерних issue (linked к #34):**
   - **#35 [P1]** — Edge Functions `auth-code`/`telegram-notify` не задеплоены в
     production (CI-доказательство этой сессии + live-пробы А-пакета).
   - **#36 [P2]** — два канала ложного зелёного в харнесе гейта (оба
     воспроизведены сырьём этой сессии).
4. **Repo-часть TASK 3/6/8/9 проверена как данные:** pg_proc inventory после
   verbatim-применения схемы, идемпотентность, RLS-пробы (10/10),
   payment-authority/hold-контур #21.
5. **Production-часть TASK 3/4/5/6/9 честно классифицирована UNKNOWN/BLOCKED**
   (песочница без egress до *.supabase.co — доказано; prod-записи без
   разрешения владельца запрещены).
6. **Карта доказательств** (`docs/ISSUE-34-TRIAGE.md`) обновлена; этот отчёт;
   конечный статус — §11.

## 3. ПРОВЕРКА

Способ и условия (всё — эта сессия, 2026-09-24 UTC, кроме явно помеченного):

- Окружение: node v22.22.3, npm 10.9.8 (свежий `npm install --no-audit
  --no-fund`), python 3.11.2, embedded-postgres 18.4.0-beta.17, Linux sandbox,
  UTC. Egress: TLS до *.supabase.co и *.github.io обрывается на handshake
  (`curl -sv …/auth/v1/health` → `SSL_ERROR_SYSCALL`, exit 35; node
  ECONNRESET; python SSLZeroReturnError); npm registry и GitHub API доступны.
- Гейт: `npm run verify` (22 набора; успех = exit-коды дочерних процессов),
  `verify_pages.py` против живого `devserver.py :8765`.
- Атаки на D1-доказательства (все — с измеренным exit-кодом через
  `${PIPESTATUS[0]}`):
  - форсированный FAIL в копии parity-набора;
  - мутант A: engine с до-фиксным grace (`windowEndMin = slotEnd`);
  - мутант B: resolver без клампа (0,480];
  - stray async crash вне цепочки try/catch (минимальная модель + копия
    реального parity-набора);
  - копия `verify_app.mjs` против модуля, бросающего при импорте.
- Схема как данные: verbatim `supabase/schema.sql` на embedded PG 18.4;
  `pg_proc`, `has_function_privilege`, повторное применение (идемпотентность);
  ad-hoc RLS-пробы от ролей anon/authenticated/foreign owner (10 штук).
- Чтение без исполнения: `create_booking` целиком, обработчики/скелеты всех
  22 наборов (heads/tails), deploy-workflow, таски оплаты #21.
- GitHub: gh api для issue/PR/workflow-runs (запись: созданы #35, #36).

## 4. ПОЛОЖИТЕЛЬНЫЕ РЕЗУЛЬТАТЫ (подтверждённые факты)

- **Гейт на проверенном SHA честен для текущего состояния:** 22/22, exit 0;
  `verify_pages.py` ALL PASS, exit 0. **[FACT]**
- **D1-реализация и enforcement подтверждены независимо:**
  - единый канонический engine; второго решателя доступности нет; 3 точки
    вызова candidate-resolver; `dayWindowEndMinutes` — только подпись. **[FACT]**
  - Сервер `create_booking` закрывает весь список политик: закрытый день,
    окно услуги, min-notice, max-advance, день/неделя лимиты, буферы,
    overflow, busy overlap, blocked period, сетка/инкремент, прошлое в поясе
    кабинета; advisory-lock сериализация; серверная деривация оплаты/статуса/
    длительности. **[FACT — чтение + исполнение на PG]**
  - Parity-матрица ловит исторические баги **точечно**: grace-мутант → ровно
    P12-REGRESS FAIL; clamp-мутант → ровно 2×P13-REGRESS FAIL; форсированный
    FAIL → exit 1. Проверка «может ли матрица быть false-green» — не
    подтверждена в этих трёх атаках. **[FACT — эксперименты]**
  - E2E конечного состояния (строки PG, busy-перепроверка обеими сторонами,
    зеркало id, wiring уведомлений) читается и исполняется зелёным;
    транспортная подмена задекларирована честно. **[FACT]**
- **#21: серверный контур подтверждён repo-side:** клиентские деньги/статус/
  длительность игнорируются (T02/T04/P17/E3); просроченный hold не держит слот
  (T05, live-эксперимент на PG). **[FACT]**
- **RLS/авторизация как данные (10/10 PASS, эта сессия):** anon_read_services
  фильтр строго доказан (неактивная невидима анониму, видима владельцу);
  ID-guessing неактивной услуги → пусто; `psychologists`/`sessions`/
  `client_risks`/`booking_attempts` закрыты для anon; cross-tenant select → 0
  строк; `public_profiles` без email/key_verifier/owner_id;
  `public_booked_slots` без client_id/оплат. **[FACT]**
- **Схема:** 1 overload create_booking (22 параметра), гранты верные, claim —
  anon denied/authenticated allowed, schema.sql идемпотентен, SR-001/004
  колонки на месте. **[FACT]**
- **#35: CI-сторона доказана полностью:** все прогоны supabase-deploy —
  success-через-skip, deploy всегда skipped. **[FACT — gh api, эта сессия]**
- **#33 state: OPEN — VERIFIED GITHUB (gh api, эта сессия).**
- **hidden auth-step-code:** `index.html:121` — форма скрыта (`class="hidden"`). **[VERIFIED REPO]**
- **`docs/ISSUE-34-TRIAGE.md` из А-пакета в worktree отсутствовал** (файл не
  пережил смену песочницы) — восстановлен этой сессией как карта доказательств.

## 5. ОТРИЦАТЕЛЬНЫЕ РЕЗУЛЬТАТЫ (ограничения и находки)

- **[FOUND → #36, P2]** Два канала ложного зелёного в харнесе (воспроизведены
  сырьём): (i) async-крах вне цепочки → `ALL PASS`, exit 0 (`finishSuite(0)`
  затирает `process.exitCode=1`); (ii) `verify_app.mjs` — нет красного канала
  (битый импорт app.js → exit 0; SPA может умереть при зелёном гейте).
  Наблюдение: guard покрывает только `tests/`.
- **[FOUND → #35, P1]** Edge Functions не задеплоены (CI не деплоил ни разу —
  FACT этой сессии; live NOT_FOUND — А-пакет). Основной путь регистрации по
  коду — down; fallback OTP — единственный и сам не доказан (#18).
- **[NOT FIXED — repo]** #21 п.4 «оплата на сайте»: по-прежнему local-only
  успех (`paySession`/`completePayment` — localStorage + Telegram-«Оплата
  прошла (сайт)», серверной записи нет). Issue #21 корректно остаётся OPEN.
- **[UNKNOWN — production]** Live-перепроверки этой сессии невозможны: нет
  egress (доказано); prod-записи без разрешения владельца запрещены. Открыты:
  R2 (POST create_booking: missing-params/PGRST203/2xx), R3 (OTP end-state),
  R4 (JWT-негативы в проде), повтор L3/L4, drift prod↔schema.sql как факт
  (repo-side «схема применяется/идемпотентна» доказан; факт применения в проде
  — только А-пакет L1/L2/L6–L8, косвенно).
- **[НЕ НЕЗАВИСИМО]** А-пакет live-проб для меня — вторичные данные; я их не
  воспроизвёл и не опроверг (см. §11 на статус-эффект).
- **[ПРИМЕЧАНИЕ]** `verify_app` smoke-лог в гейте показывает ожидаемую
  offline-ветку каталога (seed fallback) — не дефект, задокументировано.

## 6. ТУФТА НАЙДЕНА

Классификация по §1 RULES (включая самонаблюдение):

1. **(форма 6, чужая — историческая, подтверждена косвенно):** А-пакет и
   D1-REPORT честно маркируют себя АУДИТОР: САМ — самосертификации «100%
   verified» без маркера на main @ 4d490d2e не обнаружено (проверено чтением
   D1-REPORT: гейты Challenger/Main Re-Audit объявлены ОТКРЫТЫМИ).
2. **(форма 1, моя собственная — поймана до публикации):** первый прогон
   «негативного контроля» я измерил `${PIPESTATUS[0]}` неправильно (получил
   код `tail` вместо `node`) — чуть не записал ложное «exit 0 при краше
   модуля». Исправлено методикой; все числа в отчёте — с корректным каналом
   кодов. Урок: именно так рождается метрическая туфта.
3. **(форма 5 → #36):** find H2 — смоук-набор, который «проверяет» boot SPA,
   но устроен так, что не может покраснеть (B-канал). Классический
   broken-preview-класс: гейт зелёный, потенциально — мёртвая страница.
4. **(форма «зелёный CI», процессная → #35):** прогоны deploy-workflow
   «success» при пропущенном деплое — зелёный статус как ложный сигнал
   состояния контура.
5. **(форма 2 — НЕ найдена в D1):** семантической подмены цели D1 в паритете
   не обнаружено — матрица атакована мутантами и держит именно исторические
   баги; scope-ограничения (garbage-in, anti-spam асимметрия) задекларированы.

## 7. КОРЕННЫЕ ПРИЧИНЫ (5 Why)

**#35 (функции не задеплоены):** W1: NOT_FOUND → W2: не развёрнуты →
W3: CI пропускает деплой без `SUPABASE_ACCESS_TOKEN`/`SUPABASE_PROJECT_ID`,
ручной деплой не выполнялся → W4: skip завершается success — сигнал «не
задеплоено» нигде не горит → W5: активация прод-контура требует секретов
владельца, а контроля «функция отсутствует» нет. Лечение: владелец задаёт
секреты/деплоит (runbook готов, INFRA.md), далее smoke-шаг workflow + приёмка
по live-пробам (см. #35 DoD).

**#36A (маскировка async-краха):** W1: exit-код решает только `results` →
W2: обработчики не пополняют `results` (ставят exitCode) → W3: контракт «крах
вне цепочки ⇒ red» не был специфицирован → W4: фикс D1-QG-004 покрывал
beforeExit/hook, не перекрытие exitCode собственным exit(0) → W5:
отсутствует канонический инвариант «не-teardown ошибка процесса ⇒ exit≠0».
**#36B:** набор вырос из diagnose-smoke без red-канала + guard ограничен
tests/. Лечение: фикс finishSuite + red-канал verify_app + guard на все
наборы + negative control (предложения в #36).

**#21-п.4 (не закрыто):** корневая причина исходного issue верна и жива для
клиентской оплаты: серверного доверенного пути «оплата подтверждена» нет;
серверная деривация создания записи (п.1–3) уже закрыта.

## 8. РЕМОНТ

В рамках Challenger-миссии код не менялся (полномочия аудита; ремонт — через
дочерние issue, иначе сломал бы разделение Producer/Challenger):

| Дефект | Issue | Класс | Содержание ремонта |
|---|---|---|---|
| Edge Functions не задеплоены | **#35** | P1 | owner-секреты + deploy (CI/CLI), smoke, приёмка live + #18 E2E |
| Ложный зелёный: async-крах | **#36** | P2 | finishSuite почитывает exitCode; negative control |
| Ложный зелёный: verify_app | **#36** | P2 | red-канал; guard на все SUITES; сигнал в verify_all |
| Оплата local-only (#21 п.4) | (живой #21) | P1 | уже открыт; мой аудит фиксирует: п.1–3 fixed-in-repo, п.4 not-fixed |

Stop-the-Line эффект: merge/релиз деплой-контура функций (#35) закрыт до
деплоя+приёмки; код-контуры main (#36) продолжают работу с записанным риском
(гейт сужен двумя каналами до их закрытия).

## 9. ОСТАВШИЕСЯ РИСКИ

1. **Production blind spot (главный):** прод не проверен этой сессией вообще
   (E1). Всё «live»-знание — А-пакет + CI-логи. R2/R3/R4 открыты (список в
   ISSUE-34-TRIAGE N-пакете); каждый выстрел имеет точный шаблон команды.
2. **#36-каналы существуют до фикса** — внимательность к exit-кодам новых
   наборов до закрытия #36.
3. **slot_times — UI-only enforcement:** прямой RPC принимает любой старт в
   окне (запинено P10/D1.10; контракт документирован, см. R-a).
4. **public_profiles отдаёт неактивные профили** анониму; UI фильтрует — см.
   R-b.
5. **Платёжный UX:** « оплачено/подтверждено » показывается без серверного
   факта оплаты (#21 п.4).
6. **P3-пробы parity ±1 мин к границе now** — узкая flaky-полоса.
7. **Гейт только локальный** (CI отсутствует) — green нигде не хранится как
   артефакт, кроме отчётов.

## 10. ДОКАЗАТЕЛЬСТВО

SHA/env/date см. в шапке. Сырьё команд — в ISSUE-34-TRIAGE.md.

| Утверждение | Команда/путь | Результат (эта сессия) |
|---|---|---|
| origin/main не сдвинут | `git fetch` + `git log origin/main -1` | 4d490d2e…, 2026-09-24 12:40 UTC |
| Чистое дерево | `git add -A && git write-tree` vs `HEAD^{tree}` | равны (d7ee7ca…) |
| Гейт | `npm run verify` | 22/22, exit 0 |
| Страницы | `verify_pages.py` + devserver | ALL PASS, exit 0 |
| Forced-fail канал | копия parity + `check(false)` | `1 FAILED`, exit 1 |
| Grace fail-before-fix | мутант engine + полная матрица | ровно P12-REGRESS FAIL, exit 1 |
| Clamp fail-before-fix | мутант resolver + матрица | ровно 2×P13-REGRESS FAIL, exit 1 |
| Async-крах маскируется | модель + копия parity + stray throw | `ALL PASS`, exit 0 → #36 |
| verify_app без red | копия verify_app + битый import | печать ошибки, exit 0 → #36 |
| RLS как данные | ad-hoc пробы на embedded PG | 10/10 PASS |
| Схема | pg_proc/grants/повторное применение | 1 overload×22; идемпотентна |
| CI не деплоил | `gh api …/actions/runs/…/jobs` | deploy=skipped во всех прогонах |
| #33 OPEN, #21 OPEN | `gh issue view` | OPEN оба ([VERIFIED GITHUB]) |

**Claim-матрица (TASK 7):**

| Claim | Status (эта сессия) | Основание |
|---|---|---|
| repo HEAD | **VERIFIED** | git/gh, 4d490d2e |
| public catalog | **NOT RE-VERIFIED** (А-пакет LIVE L5/PAGE) | egress blocked (E1); my FACT: code seed-fallback |
| 5 services | **NOT RE-VERIFIED** (А-пакет L8); repo-строгость фильтра **VERIFIED** | RLS-проба RLS1/RLS2 |
| OTP 200 (GoTrue health) | **NOT RE-VERIFIED** (А-пакет L9/L10) | egress blocked |
| auth-code 404 | **NOT RE-VERIFIED** (А-пакет L3); CI-сторона **VERIFIED** | jobs api; #35 |
| create_booking PGRST203 | **NOT VERIFIED / UNKNOWN** | live POST запрещён средой; repo: 1 overload [FACT] |
| hidden auth-step-code | **VERIFIED REPO** | index.html:121 `class="hidden"` |
| D1 implementation | **VERIFIED REPO (независимо)** | §4 |
| production registration | **NOT PROVEN** | #35 + E1; fallback-OTP end-state не доказан |
| #33 state | **VERIFIED GITHUB** | OPEN, P1, STOP THE LINE |

**Разделение:** всё выше — FACT (эта сессия) / LIVE (А-пакет, помечено) /
INFERENCE (page-рендер=seed картина — из кода) / UNKNOWN (весь live).

## 11. ОКОНЧАТЕЛЬНЫЙ СТАТУС

**ЧАСТИЧНО ЗАВЕРШЕНО.**

- Выполнено (независимо, с сырьём): TASK 1, TASK 2 (deep, атаками), TASK 7,
  TASK 8 repo-часть, TASK 9 repo-часть, TASK 10 в объёме локального main
  (гейт+страницы+соседние/негативные сценарии+DRY+канон на чистом SHA),
  TASK 11 (ниже), дочерние issue #35/#36, карта доказательств, отчёт.
- Заблокировано средой (задекларировано, не выдумано): production-ноги
  TASK 3/4/5/6/9 (R2–R4) + повтор live L1–L10.
- P1 #35 обязывает держать Quality Gate #12 ОТКРЫТЫМ (Stop-the-Line для
  деплой-контура); #33 НЕ закрывается этим аудитом автоматически (локальный
  green + независимый Challenger выполнены, но Main Re-Audit production —
  нет).
- Терминальный «ЗАВЕРШЁННЫЙ» невозможен: mandatory prod-проверки не закрыты.

**TASK 11 — Producer/Challenger separation:** Producer проверяемой работы —
сессия PR #32 (`arena/01a0d32e`, АУДИТОР: САМ по её собственному заявлению).
Challenger — эта сессия (`arena/01a0d3a7`), отдельный агентный процесс: созданный
код/тесты/документы PR #32 мной не писались и не рецензировались до этого
аудита; доказательства собраны с нуля на моей машине (гейт, мутанты, пробы,
gh api). Не независимо: (а) файловая база — тот же git-объект (это и есть
предмет проверки, ок); (б) А-пакет live-проб получен через миссионный пакет —
использован только как «заявлено», ни один вывод не опирается на него без
маркера; (в) «независимый Challenger» самого моего отчёта отсутствует — его
проверка открыта следующему агенту.

## 12. УВЕРЕННОСТЬ

Высокая в §4 (каждое утверждение — с командой, сырым выводом и отрицательным
контролем канала измерения: я поймал и исправил собственную ошибку измерения
exit-кода, прежде чем публиковать числа); высокая в §5-находках (#35 — FACT
через CI API; #36 — воспроизведение сырьём в двух формах); нулевая в live-
production (UNKNOWN объявлен явно, ничего не дозволено). Это не
самовнушение: (1) мутанты доказали, что доверенная матрица способна быть
красной; (2) мои находки идут ПРОТИВ комфортного вывода «всё зелёное»;
(3) недостижимое записано недостижимым. Спорить можно с классификацией #36
как P2 (прецедент D1-QG-004 был P1) и с оценкой slot_times — обе открыто
помечены как решения/суждения, не факты.
