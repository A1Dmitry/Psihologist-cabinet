# Отчёт Агента 1 — «Инфраструктура и интеграция»

> Дата: 2026-09-23. Ветка: `arena/01a0cdb4-psihologist-cabinet`.
> Следующий: Агент 2 «Публичная запись и SEO».

## Часть A — порядок в репозитории

### 1. Дубли файлов — устранены ✅

| Пара | Решение | Обоснование |
|------|---------|-------------|
| `authService (2).js` / `authService.js` | оставлен `authService.js`, дубль удалён | импортируется в `js/app.js`; актуальная серверная авторизация (auth_login_codes + Edge Function auth-code + OTP-фолбэк), дубль — старая локальная реализация кодов |
| `nicknameService (2).js` / `nicknameService.js` | оставлен `nicknameService.js`, дубль удалён | импортируется `BookingViewModel`; логика идентична, отличия только в комментариях |
| `BaseViewModel (2).js` / `BaseViewModel.js` | дубль удалён | файлы побайтово идентичны |
| `PortalViewModel (2).js` / `PortalViewModel.js` | оставлен `PortalViewModel.js`, дубль удалён | импортируется в `js/app.js`; актуальная версия с мультисловным поиском и нормализацией ё→е |

Коммит: `chore: remove duplicate files with ' (2)' suffix`. Ни одна ссылка в коде на «(2)»-файлы не вела (проверено grep по всему дереву).

### 2. Перенос репозитория — решение зафиксировано ✅ (с открытым вопросом)

- Проверено: `mikhailouskayanataliya-collab/Psihologist-cabinet` через GitHub API
  **недоступен (404)** — довести перенос до конца технически невозможно
  (приглашение коллаборатору не принято / репозиторий недоступен).
- Принято решение (б): **`A1Dmitry/Psihologist-cabinet` — основной и единственный
  репозиторий**. `COLLAB.md`/`TRANSFER.md` перенесены в `docs/archive/repo-transfer/`
  со статус-заметкой и инструкцией по возобновлению переноса; README обновлён.
- **Открытый вопрос владельцу продукта:** нужен ли перенос вообще? Если да —
  принять/выдать доступ по инструкции из архива (далее mirror-push + перенос
  Pages/Sекретов). До ответа работаем здесь.

### 3. Baseline тестов (стартовая точка для QA) ✅

Прогон на коммите `ff81b0c` (до моих изменений) и повторный контроль после всех
коммитов — результаты идентичны:

| Скрипт | Результат | Детали |
|--------|-----------|--------|
| `verify_app.mjs` | ✅ PASS | «IMPORT OK» + «BOOT RAN», exit 0; (stacktrace «catalog load failed» в выводе — ожидаемый путь ошибки при моке fetch) |
| `verify_auth.mjs` | ✅ 25 PASS / 0 FAIL | «ALL PASS» |
| `verify_profile.mjs` | ✅ 29 PASS / 0 FAIL | «ALL PASS» |
| `verify_telegram.mjs` | ✅ 57 PASS / 0 FAIL | «ALL PASS (57)» |
| `verify_pages.py` | ✅ 11 PASS / 11 | локальный `devserver.py 8765`, `BASE_URL=http://127.0.0.1:8765` |

Примечание: README декларировал «26 проверок» для verify_auth — фактически счётчик
даёт 25 строк PASS, 0 FAIL. QA-агенту сверяться с фактическим счётом.

## Часть B — продакшен-инфраструктура

### 4. Прод-проект Supabase ✅ подтверждён декларативно

Проект существует и зашит в `js/services/supabaseConfig.js`:
`https://phiavtroybgwyjdhqqkh.supabase.co`, anon key свежий (выпущен 2026-09-21).
Прямая проверка из песочницы невозможна (egress до `*.supabase.co` заблокирован,
access-токена владельца у агента нет) — **финальное подтверждение за владельцем**
по чек-листу `docs/INFRA.md`.

### 5. Схема и seed — решение принято ✅, применение — за владельцем ⏳

`schema.sql` идемпотентен, покрывает все сущности + RLS + view + RPC.
**Решение по seed: накатывать `seed.sql` в прод** — это реальный референс-профиль
Наталии Михайловской с сайта-первоисточника, а не фиктивное демо; демо-режим в
приложении отдельный и в БД не ходит. ⚠️ Задокументирована «ловушка первого
входа» (до логина заменить технический `email` у `psy_catalog_19` на настоящий,
иначе claim создаст дубликат профиля) — шаг 2 в `docs/INFRA.md`.

### 6. Edge Functions ✅ готовы к деплою, ⏳ деплой за владельцем

- `supabase/config.toml`: `verify_jwt=false` для обеих функций (критично: обе
  вызываются до входа/анонимно; с JWT-gateway по умолчанию клиент получал бы 401).
- Новый workflow `.github/workflows/supabase-deploy.yml`: автодеплой при мерже в
  main, как только заданы Secret `SUPABASE_ACCESS_TOKEN` + Variable
  `SUPABASE_PROJECT_ID`; без них — graceful skip с инструкцией; post-deploy
  reachability-смоук (не 404/401).
- Ручной путь (CLI) описан в `docs/INFRA.md` шаг 5.

### 7. Секреты ⛔ блокер на владельце (как и предупреждал бриф)

- `RESEND_API_KEY` — нельзя сгенерировать без аккаунта Resend владельца;
  **обязательно с верифицированным доменом**, иначе письма только владельцу
  аккаунта Resend (блокер T-15). Инструкция — `docs/INFRA.md` шаги 3–4.
- `MAIL_FROM` — после верификации домена; дефолт `onboarding@resend.dev`
  оставлен в коде осознанно (fallback-режим сам диагностирует ошибку отправки).
- В коде/репо секретов нет; площадки для значений: Supabase Edge Secrets +
  GitHub Secrets/Variables.

### 8. CI Pages ✅ улучшен и проверен

- Сборка `_site`, подстановка `%BASE%` — как было; добавлено:
  - **статические маршруты**: `/cabinet`, `/auth`, `/booking-done`, `/reply` +
    `/psy/<slug>` и `/book/<slug>` для каждого активного специалиста (из
    `public_profiles`) раскладываются реальными папками → GitHub Pages отдаёт
    deep-links с **HTTP 200** (раньше — только 404.html-fallback со статусом 404:
    для SEO это выпадение из индекса — важно для Агента 2);
  - выгрузка слагов общая для sitemap и маршрутов; без GitHub-секретов всё
    деградирует к прежнему поведению;
  - post-deploy advisory-смоук `verify_pages.py` против задеплоенного URL.
- Проверка: сгенерированный `_site` прогнан через `verify_pages.py` на простом
  статическом сервере — **11/11 PASS** без всякого fallback-серверного кода.
- `npm-publish.yml` удалён (шаблон без package.json).
- Кастомный домен: опцию оставил владельцу, инструкция (CNAME + правка `%BASE%`) в `docs/INFRA.md`.

### 9. Логирование фронтенд-ошибок ✅

- `client_error_logs` в `schema.sql`: RLS — только insert для anon/authenticated,
  select-политики нет (читается из SQL Editor/дашборда).
- `js/services/errorLogService.js` (новый): fire-and-forget, лимит 5/страницу,
  дедупликация, усечение полей, никогда не падает.
- Подключено: inline-обработчики `index.html` (boot/error/rejection — рядом с
  существующим баннером) + падение загрузки каталога в `js/app.js` (level=catalog).
- `ClientErrorLog` добавлен в `entities.js` для Code First паритета (server-only).
- Запрос на просмотр и регламент чистки — в `docs/INFRA.md`.

## Часть C — процесс для следующих агентов ✅

- `docs/SCHEMA-REQUESTS.md`: правила (чьи файлы, формат SR-XXX, приоритеты,
  пример, регламент интеграции «в мой раунд») + пустая очередь, ждущая первую
  заявку Агента 2. Единственный инфра-файл, открытый на редактирование всем.
- `docs/INFRA.md`: runbook + статус-таблица прода + наблюдения.
- README: добавлены ссылки на оба документа.

## Итог для Агента 2

- Репозиторий чист, тесты зелёные (см. baseline), процесс заявок открыт.
- Против прода писать фичи можно: схема финализирована, публичные view/RPC стабильны.
  **Но:** до применения владельцем шагов 1–5 из `docs/INFRA.md` прод «живой»
  только частично (вход по коду и realtime-уведомления заработают после деплоя
  функций + Resend). Для верификации SEO-фич настоятельно рекомендую добить
  шаги 1–2 (schema+seed в проде) и GitHub Secrets `SUPABASE_URL`/`SUPABASE_ANON_KEY`
  — тогда CI начнёт генерировать статические `/psy/<slug>` с HTTP 200.
- Заявки на новые поля/таблицы — в `docs/SCHEMA-REQUESTS.md`.

## Открытые вопросы владельцу продукта

1. Перенос в `mikhailouskayanataliya-collab/...` нужен? (см. `docs/archive/repo-transfer/`)
2. Resend: есть ли домен, который верифицируем для почты? (блокер T-15)
3. Кастомный домен для Pages — планируется? Если да — до SEO-разгона Агента 2.
