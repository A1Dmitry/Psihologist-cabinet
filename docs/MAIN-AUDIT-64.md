# MAIN-AUDIT-64 — Main Re-Audit по issue #64 (мобильные диалоги/CTA, двойная отправка)

> **АУДИТОР: САМ** (тот же конвейер, что и исправление — сессия Arena Agent Mode,
> `EXEC-zIPWd7Mtyl`). Независимый Challenger (§6.6) в песочнице недоступен; это
> ограничение зафиксировано, а не замаскировано. Проверка выполнена **после merge**,
> на снимке актуального `main`, не на ветке PR (§6.7 п. 1–2).

## 1. Проверенный снимок

| Параметр | Значение |
|---|---|
| `main` SHA | `a4675537e1e783f6fa79f7bc8068a6599dcc1a87` |
| Что это | merge PR #97 (`fix(#64)…`), 2026-09-26T12:08:10Z |
| Предыдущий main | `5d4f68d` (merge PR #96) |
| Окружение | node v22.22.3; отдельный worktree `git worktree add --detach /tmp/main64 a467553` (закреплённая ветка сессии не переключалась, §6.7 п. 1); `devserver.py` порт 8791 |
| Дата/время аудита | 2026-09-26, 12:09–12:12 UTC |
| Production | **не проверялся**: egress песочницы до `*.supabase.co` и живого сайта закрыт; произвольный снимок production не выдумывается (§6.15) |

## 2. Исправление присутствует в этом SHA (п. 2)

```text
git cat-file -s a467553:js/views/uiDialogs.js   → 8446 bytes   (новый канонический модуль)
git cat-file -s a467553:tests/mobile-ux.mjs     → 23053 bytes  (регресс-набор в гейте)
git cat-file -s a467553:docs/ISSUE-64-REPORT.md → 21130 bytes
main:index.html — 11 вхождений маркеров #64 (viewport-fit=cover, modal-panel,
                  book-cta, ui-confirm-ok)
main:js/**       — вызовов нативных confirm()/prompt() нет; совпадения только
                  в комментариях («замена нативных confirm()/prompt()»)
```

`git diff HEAD a467553 -- <затронутые пути>` от ветки PR — **пусто** (содержимое
идентично; в main после merge других правок этих файлов нет).

## 3. Повторение исходных сценариев дефекта (п. 3)

Полный гейт на снимке main:

```text
/tmp/main64 $ npm run verify
→ Все наборы зелёные (39); проверок предъявлено: 1737; exit 0
   (tests/mobile-ux.mjs — 41 проверка, tests/booking-wizard.mjs — 56,
    tests/harness-guard.mjs — 283)
```

Негативные/соседние сценарии на **том же снимке**:

```text
1) guard отключён в снимке (if (false && this.submitting)) →
   4 FAILED → exit 1
   FAIL BL-05: повторный submit() не создаёт вторую заявку
   FAIL BL-05: серверный вызов ровно один → 2
   FAIL BL-05: в локальном зеркале одна новая заявка → 0 → 2
   (исходный дефект BL-05 воспроизводится: два вызова, две заявки)
2) нативный confirm() возвращён в js/app.js →
   FAIL MX-03: нативных confirm()/prompt() в js/ нет → js/app.js: confirm(
3) снимок восстановлен → ALL PASS
```

Живой сервер из снимка main (`python3 devserver.py 8791`):

```text
BASE_URL=http://127.0.0.1:8791 python3 verify_pages.py → ALL PASS (13/13)
GET /js/views/uiDialogs.js          → 200, содержит 3 экспорта (uiConfirm/uiPrompt/copyText)
GET /book/{slug}                    → в разметке id="book-cta" class="book-cta …"
GET /                               → app.js?v=20260926-mobile (cache-bust актуален,
                                      старой сборки браузеру не отдать)
```

CI и доставка:

```text
PR #97 checks: probe=pass, verify=pass
Deploy to GitHub Pages (push main, run 36240974811, sha a467553) → success (1m3s)
```

Деплой подтверждает сборку/публикацию артефакта, но **не** является проверкой
поведения в браузере (§6.1) и не заменяет production-наблюдение.

## 4. Дублирующая логика и границы (п. 3)

- Подтверждения/ввод/копирование существуют в единственной реализации
  (`js/views/uiDialogs.js`); в `js/app.js` и `js/views/cabinetUi.js` — только вызовы.
- Бизнес-правила не переопределялись: `submitting` — presentation-флаг, порядок
  `validate → server transaction → persist → success` в `BookingViewModel.submit()`
  сохранён (RULES §6.14); схема БД / RPC / Edge Functions не тронуты.
- Мобильные стили живут в `css/tailwind.src.css` и попадают в сборку штатной
  командой (`verify_tailwind` — 361 класс найден); ручных правок `css/tailwind.css`
  нет (в сборке custom-правила идут после утилит — при равном весе перекрывают
  `.items-center`/`.max-w-md`/`.max-h-[90vh]`, проверено по позициям в файле).

## 5. Незакрытые/непроверенные сценарии (честно)

1. **Мобильный рендер на реальном устройстве** — не проверен (браузера/эмулятора
   в песочнице нет). Остаётся за владельцем: `/cabinet` и `/book/{slug}` на телефоне.
2. **Production** — не вызывался; egress закрыт. Проверено только: deploy-ран
   GitHub Pages success и наличие артефактов в снимке `main`.
3. **Независимый Challenger (§6.6)** — недоступен в песочнице; повторный проход
   Producer независимым аудитом не считается.
4. Focus trap в sheet-диалогах не реализован (вне DoD #64) — остаточный риск.

## 6. Итог по §5 RULES

- Разделы 1–4: цель #64 и её repo-результат; проверка описана выше командами и выводами.
- Раздел 11 (статус): **ЧАСТИЧНО ЗАВЕРШЕНО** — исправление подтверждено на актуальном
  `main` (`a467553`), но независимый Challenger и устройство владельца открыты.
- Раздел 12 (уверенность): высокая по repo-фактам (SHA, файлы, прогоны, негативные
  контроли воспроизводимы), нулевая по production — он не измерялся.
- Kaizen/стандартизация: синхронизация `docs/CURRENT-STATE.md` выполнена в том же
  цикле (см. отдельный PR после этого документа, §6.7 п. 6); сигнал RRSI внесён
  в реестр `docs/RRSI-CACHE.md` и ждёт независимого Challenger.

**Условие для закрытия #64:** независимый Challenger по §6.6 (или решение
сопровождающего о его недоступности) + подтверждение владельцем на устройстве.
Merge и зелёный CI закрытием не являются.
