# RRSI-CACHE — кэш правильных решений и невынужденных ошибок

> Единственный канонический источник накопленного опыта исполнителей
> (правило [`docs/RULES.md`](RULES.md) §7, RRSI).
>
> **Вход в задачу (§7.1):** найти записи своего контура по тегам/«ситуации»
> и применить их наравне с правилами. **Выход из цикла:** сохранить новый урок
> по §7.3 либо явно заявить «новых уроков нет» с обоснованием.
> Повторение зафиксированной здесь ошибки — нарушение дисциплины (§7.5).
>
> Обновление записей — только по §7.3: фактический сигнал → проверка
> на исходном случае → независимый Challenger → фиксация с SHA, датой
> и маркером аудитора.

## Формат записи

```
### RRSI-NNN — <краткое имя>
- Статус: ACTIVE | CONFIRMED | СТАНДАРТИЗОВАНА | REPEALED (дата, причина)
- Контур/теги: <для поиска на входе в задачу>
- Ситуация: признак узнавания — в каких задачах/изменениях запись обязательна.
- Антипаттерн: ошибка и её корневая причина (кратко).
- Правильное решение: канонический паттерн, ссылки на код/документ.
- Доказательство: источник-сигнал, SHA, дата, АУДИТОР (по §3).
- Журнал применения: дата — задача — результат (для CONFIRMED нужно ≥ 2).
```

Статусы: **ACTIVE** — проверена на исходном случае; **CONFIRMED** — ≥ 2 успешных
применения в независимых циклах; **СТАНДАРТИЗОВАНА** — норма перенесена
в `RULES.md` / автотест / Poka-Yoke, здесь остаётся ссылка; **REPEALED** —
отменена при изменении контракта/контекста (причина и дата обязательны).

---

## Записи

### RRSI-001 — Канал доставки кода входа переживает перезагрузку и отказы

- **Статус:** ACTIVE (восстановлена из документированной истории репозитория
  2026-09-25; применения до восстановления в журнале не отражены).
- **Контур/теги:** `auth`, вход по одноразовому коду, `auth-code`, Edge Function,
  OTP, UI-состояние ожидания кода.
- **Ситуация:** любые изменения входа психолога (страницы «Вход», «Каталог»,
  генерация/проверка кодов, транспорт доставки, состояние ожидания кода в UI).
- **Антипаттерн:** выбранный транспорт доставки терялся при F5; при временном
  отказе Supabase Auth код сгорал, и пользователь попадал в тихий перебор
  каналов. Корневая причина: состояние канала и жизненный цикл кода
  не переживали сбой и перезагрузку.
- **Правильное решение:** транспорт и окно ввода сохраняются и восстанавливаются
  после перезагрузки; код проверяется только тем каналом, которым выслан;
  порядок на сервере — атомарный захват строки кода → создание сессии →
  гашение (`used_at`); при отказе Auth захват снимается и код можно ввести
  снова; при истёкшем окне — «Запросите новый код». См. `README.md`
  («Вход психолога», свойства из issue #14).
- **Доказательство:** источник — дефект/исправление issue #14; отчёты
  `docs/ISSUE-14-REPORT.md`, `docs/ISSUE-14-CHALLENGER.md`; контрактные тесты
  `tests/registration-flow.mjs`, `tests/auth-code-edge.mjs`,
  `tests/auth-ui-pending.mjs`. АУДИТОР: ВНЕШНИЙ — независимый челлендж-отчёт
  `docs/ISSUE-14-CHALLENGER.md` (состав записи восстановлен по документации
  репозитория, 2026-09-25, без новой независимой проверки — новый аудит
  требуется при изменении контура, §7.4 R7).
- **Журнал применения:** пусто.

### RRSI-002 — «Готово» для UI только после смоука реального сервера

- **Статус:** СТАНДАРТИЗОВАНА — норма закреплена в `RULES.md` §1 п. 5
  (форма «туфты»: поломка окружения/предпросмотра) и в преамбуле `AGENTS.md`;
  в кэше сохранена операционная механика.
- **Контур/теги:** `ui`, маршруты, страницы, предпросмотр, `devserver`,
  смоук-проверки.
- **Ситуация:** любая задача, затрагивающая страницы/маршруты/рендер
  (`index.html`, `js/views`, `js/viewmodels`, роутинг, `devserver.py`).
- **Антипаттерн:** задача объявляется решённой по зелёным юнит-проверкам в JS
  при сломанном приложении/предпросмотре; смоук прогоняется не против реального
  сервера. Корневая причина: проверяется наличие кода, а не наблюдаемое
  поведение страниц.
- **Правильное решение:** «готово» только после `python3 verify_pages.py`
  против реально поднятого `python3 devserver.py 8765` (локально) или против
  развёрнутого контура (`BASE_URL=…`); при изменении воронки записи —
  дополнительно `node tests/booking-wizard.mjs`.
- **Доказательство:** норма и механика зафиксированы в `AGENTS.md`
  («Правило целостности превью…») и `README.md` («Запуск», «Смоук маршрутов»).
  АУДИТОР: САМ — запись восстановлена из действующих документов 2026-09-25.
- **Журнал применения:** норма действует для всех задач с изменением UI.

---

## Реестр сигналов без записей

Сюда вносятся фактические сигналы (дефекты, контрпримеры), по которым запись
ещё не создана, — чтобы сигнал не потерялся до завершения цикла §7.3:

| Дата | Сигнал (ссылка) | Статус | Владелец |
|------|-----------------|--------|----------|
| 2026-09-25 | issue #74: первый визит /book/{slug} — «Специалист не найден» навсегда. Сигнал: все существовавшие мок-DOM харнесы не видели дефект, т.к. не моделируют уничтожение дочерних узлов при перезаписи `innerHTML` контейнера (найден только браузерным E2E в #65). Черновик записи (рабочее имя RRSI-003) подготовлен исполнителем в комментарии/отчёте по issue #74 и ждёт независимого Challenger-а по §7.3 — самосертификация запрещена (§3, R3). (Тот же сигнал, что в следующей строке; при создании записи консолидировать по R8.) | черновик ждёт Challenger | EXEC-sbcg33SVoK |
| 2026-09-25 | Юнит-зелёная воронка скрывала дефект первого визита: E2E в реальном браузере (#65) показал, что `/book/{slug}` при пустом кэше остаётся «Специалист не найден» после загрузки каталога (`renderBooking` затирает `#book-body`) — [#74](https://github.com/A1Dmitry/Psihologist-cabinet/issues/74). Кандидат в урок: у DOM-стабов нет «разрушения узлов», для deep-link нужен браузерный E2E с пустым кэшем. | NEW (ждёт проверки на исходном случае + Challenger) | EXEC-sN15uzVCKR |
| 2026-09-25 | #65 решён дважды параллельно (PR #73 и #75): CLAIM в Issue не опубликован (403 у интеграции), второй исполнитель видел FREE. Затем «Update branch» слил два генератора → `SyntaxError: ICS_PRODID` на голове PR #75 (сайт бы не загрузился). Кандидат в урок: claim с exit 4 = не начинать работу молча; перед merge — `git fetch` + сверка `main` на пересечение по Issue и полный гейт на фактической голове PR. | NEW (ждёт проверки + Challenger) | EXEC-sN15uzVCKR |
| 2026-09-26 | #64 (MX-03/MX-05/MX-06/BL-05): presentation-инвариант нельзя закрывать проверкой одного уровня — флаг `submitting` в ViewModel зеленит тест, пока кнопка «Записаться» остаётся кликабельной; а статический контроль «нативных `confirm()/prompt()` нет» обязан исключать комментарии (иначе краснеет на документации и ослабляется до бесполезного). Кандидат в урок: «UI-инвариант = состояние модели + состояние элемента управления в рендере + статический запрет формы-предшественника». Третий сигнал цикла: фолбэк заменённого нативного диалога обязан быть тем же UI-примитивом (sheet), иначе MX-03 «лечится» в одном месте и возвращается в другом. | черновик проверен на исходном случае (fail-before-fix: guard off → 2 вызова сервера/2 заявки; native confirm → FAIL) ждёт независимого Challenger | EXEC-zIPWd7Mtyl |
| 2026-09-26 | #63: три silent-артефакта fuzzy-правок при статусе «success» (2 мусорных фрагмента в `js/app.js`, 2 неприменённые правки); пойманы только перепроверкой ground truth (`node --check`, grep, тесты), ни один не ушёл из рабочей ветки. Кандидат в урок: «success» инструмента правок не является доказательством — каждая правка сверяется чтением изменённого места до следующего шага; повторные правки одного файла — детерминированным патчем. | NEW (ждёт проверки + Challenger) | EXEC-ZLCS0Evg7b |
| 2026-09-26 | Ре-аудит #63 на `c6c2f2e` (после PR #103, который менял `index.html`/`js/app.js` уже ПОСЛЕ Main Re-Audit на `8c96311`) нашёл 2 функциональных дефекта мобильной раскладки, пропущенные и 59 проверками набора `tests/mobile-cabinet.mjs`, и Challenger-аудитом той же сессии (`docs/ISSUE-63-CHALLENGER.md` — «дефектов не обнаружено»): (1) панель «…» закрывалась только по тапу ВНЕ `.cab-menu`, поэтому пункт без перерендера (`<a>«В календарь»`) и отмена `uiConfirm`/`uiPrompt` («Удалить»/«Неявка») оставляли панель с деструктивным пунктом открытой; (2) `.cab-menu{z-index:30}` < `.cab-tabbar{z-index:40}` (fixed) — у нижних строк списка панель открывалась ПОД таб-баром, тап уходил в навигацию. Кандидат в урок: «инвариант всплывающего элемента = полный набор сценариев закрытия (тап вне, тап по пункту, Escape, перерендер, навигация) + относительный z-порядок против фиксированной навигации, проверенный и в исходнике стилей, и в артефакте сборки». Третий сигнал того же цикла: новый поведенческий тест дал **ложное зелёное** — предусловие «панель открыта» не проверялось, и `toggleCabMenu` на открытой панели просто закрывал её (тапа не было, а PASS есть); предусловие обязано входить в условие проверки. Четвёртый сигнал: комментарий-оправдание с посторонним именем человека и ложной посылкой (id генерируются base36 — `js/core/dbContext.js:15`) жил в main незамеченным. Evidence: fail-before-fix 4 FAIL/61 PASS → после фикса 65 PASS, гейт 40/40 (1803→1809), смоук 13/13 — `docs/ISSUE-63-REAUDIT.md`. | черновик: применён и проверен на исходном случае (fail-before-fix показан); ждёт независимого Challenger (§7.3) | EXEC-rzVmS30TVl |
| 2026-09-26 | Гейт `verify_cabinet` красный по проверке «встречи серии стоят в расписании» в Сб/Вс/Пн/Вт и зелёный в Ср/Чт/Пт: тест берёт `vm.series[length-1]` из **сортированного** `list()` (последний = «удачный» weekday), новая серия планируется на `weekdayOf(today+9)` — дата-зависимая фикстура, ложное зелёное в «удачные» дни (второй дата-флейк после `notice-граница` PR #81). Черновик записи (рабочее имя RRSI-004: «пинить today; идентифицировать сущности по id результата, а не по позиции в списке; дата-чувствительные наборы гонять в ≥2 дня недели») — `docs/ISSUE-50-REPORT.md` §8, ждёт независимого Challenger-а по §7.3. Дефект оформлен: issue #92. **Обновление 2026-09-26 (цикл #92, `EXEC-glxK1b-Av6`):** черновик применён и проверен на исходном случае; найден контрпример к правилу «≥2 дней» — фикс только по id зеленел 6 дней из 7 (второй механизм: `plus(9)` по понедельникам попадал на слот «Ср 09:00» серии из секции 7). Уточнённый черновик («все 7 дней недели через инъекцию часов `tools/fake-clock.mjs`») — `docs/ISSUE-92-REPORT.md` §8. | черновик уточнён, проверен на исходном случае; ждёт внешнего подтверждения | EXEC--sg3Nf8dnc → EXEC-glxK1b-Av6 |
| 2026-09-26 | #121 (R14): демо «выключили» кнопкой (`enableDemoData`), но seed-фикстура `dbContext` всё равно показывалась на портале **по времени** — в переходном состоянии `loading` первого визита (проба: 19 карточек фикстуры до ответа сервера). Ни один из 40 наборов гейта этого не видел: все харнесы роняли `fetch` синхронно, окна `loading` не существовало. Кандидат в урок: инварианты «строго серверного режима» (и вообще запрещённых состояний) проверять в **переходных** состояниях машины, удерживая сеть гейтом (`await networkHeld`), а не только в терминальных `server`/`none`; закреплено проверкой B0 `tests/production-ui-no-demo.mjs`. Также повторно подтверждён урок #64 (статические проверки — по коду без комментариев). `docs/ISSUE-121-REPORT.md` §6–§8. | черновик применён на исходном случае; ждёт независимого Challenger-а по §7.3 | EXEC-yNQLsw2WGb |
